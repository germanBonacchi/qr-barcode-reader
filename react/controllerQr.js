//import { useFullSession } from 'vtex.session-client'

const controllerQr = (qr, format, splitPosition) => {
  /*const {
    loading: sessionLoading,
    data: { session = undefined } = {},
    error: sessionError,
  } = useFullSession()

  if(data){
    console.log(data)
  }
  */
  console.log(qr) 
  //window.location.replace('/admin/supervisor')
  return qr
}

export default controllerQr